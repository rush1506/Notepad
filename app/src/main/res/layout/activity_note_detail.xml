<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.learning.tata.notepad.note_detail">

    <HorizontalScrollView
        android:id="@+id/scrollTitle"
        android:layout_width="200dp"
        android:layout_height="50dp">

        <EditText
            android:id="@+id/detailNoteTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:hint="There is supposed to be a title here"
            android:textSize="25sp"
            android:textStyle="bold" />

    </HorizontalScrollView>

    <TextView
        android:id="@+id/detailNoteDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="19sp"
        android:layout_toRightOf="@id/scrollTitle"
        android:text="Date"
        android:textSize="15sp"
        android:textStyle="italic" />

    <EditText
        android:id="@+id/detailNoteContent"
        android:layout_width="match_parent"
        android:layout_height="480dp"
        android:layout_below="@id/scrollTitle"
        android:hint="If you're seeing this, you haven't entered anything :))"
        android:textSize="20sp" />

    <Button
        android:id="@+id/detailDeleteButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_centerHorizontal="true"
        android:backgroundTint="@color/colorDeleteButton"
        android:text="Delete" />

    <Button
        android:id="@+id/detailChangeButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:backgroundTint="@color/colorSaveButton"
        android:text="Change" />


</RelativeLayout>
